{
  "name": "price-tracker-extension",
  "version": "1.0.0",
  "description": "Track prices across any online store and get browser notifications when they drop.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}